version: '3.8'

services:
  database:
    environment:
      - POSTGRES_PASSWORD=1234
      - POSTGRES_USER=postgres
    ports:
      - "5432:5432"
    volumes:
      - database-data:/var/lib/postgresql/data